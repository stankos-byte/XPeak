# Firebase Configuration
# IMPORTANT: Copy this file to .env (NOT .env.local) for production
# For local development, you can use .env.local
# Get these values from: Firebase Console → Project Settings → General → Your apps

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id

# Firebase Emulators (Local Development)
# Set to 'true' for local development with emulators
# Set to 'false' for production deployment (IMPORTANT!)
VITE_USE_FIREBASE_EMULATORS=true

# Polar.sh Configuration
# Billing is handled via Firebase Functions
# You need to set up the POLAR_ACCESS_TOKEN secret in Firebase
# To deploy the secret: firebase functions:secrets:set POLAR_ACCESS_TOKEN
# Get your access token from: https://polar.sh/settings
