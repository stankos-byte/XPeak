# ==========================================
# XPeak Environment Configuration Template
# ==========================================
# SECURITY: This is a TEMPLATE file with no real values
# 
# Setup Instructions:
# 1. Copy this file to .env.local for local development
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit .env.local or .env files to git
# 4. Rotate credentials immediately if accidentally exposed
#
# For more info: See README.md → Security section
# ==========================================

# ==========================================
# Firebase Configuration (REQUIRED)
# ==========================================
# Where to get these values:
# 1. Go to: https://console.firebase.google.com/
# 2. Select your project
# 3. Go to: Project Settings → General → Your apps (Web)
# 4. Copy the config values from firebaseConfig object

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id

# ==========================================
# Firebase Emulators (Local Development)
# ==========================================
# Set to 'true' to connect to local Firebase Emulators
# Set to 'false' for production deployment (IMPORTANT!)
# 
# To start emulators: firebase emulators:start
VITE_USE_FIREBASE_EMULATORS=true

# ==========================================
# Server-Side Secrets (Firebase Functions)
# ==========================================
# These secrets are NOT stored in .env files!
# They are managed through Firebase Functions secrets.
#
# To set up (run in functions/ directory):
# 1. Gemini API Key:
#    firebase functions:secrets:set GEMINI_API_KEY
#    Get from: https://aistudio.google.com/app/apikey
#
# 2. Polar Access Token:
#    firebase functions:secrets:set POLAR_ACCESS_TOKEN
#    Get from: https://polar.sh/settings (under API keys)
#
# 3. Polar Webhook Secret:
#    firebase functions:secrets:set POLAR_WEBHOOK_SECRET
#    Get from: https://polar.sh/settings/webhooks
#
# To list all secrets:
#    firebase functions:secrets:list

# ==========================================
# Development/Testing (Optional)
# ==========================================
# These are only used in emulator mode for testing
# Uncomment and set if needed for local development

# VITE_APP_URL=http://localhost:5173
# VITE_TEST_PASSWORD=testpassword123
# VITE_MOCK_GOOGLE_PASSWORD=google-mock-password-123

# ==========================================
# Security Reminders
# ==========================================
# ✅ DO:
#   - Keep .env.local in .gitignore
#   - Rotate credentials if exposed
#   - Use different credentials for dev/staging/prod
#   - Run pre-commit hooks (gitleaks)
#
# ❌ DON'T:
#   - Commit .env files to version control
#   - Share credentials in chat/email/Slack
#   - Use production keys in development
#   - Hardcode secrets in source code
# ==========================================
